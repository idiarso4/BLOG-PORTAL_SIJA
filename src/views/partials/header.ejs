<!DOCTYPE html>
<html lang="id">
<% /* Head start + common CSS */ %>
<%- include('../partials/head-start', { title, description, keywords }) %>
<style>
  /* Common small utilities can go here */
  .container-narrow { max-width: 960px; }
  /* Focus visibility for keyboard users */
  :focus-visible { outline: 3px solid #0d6efd; outline-offset: 2px; }
  /* Skip link styling */
  .skip-link {
    position: absolute;
    top: -40px;
    left: 0;
    background: #0d6efd;
    color: #fff;
    padding: 8px 12px;
    z-index: 1050;
    border-radius: 0 0 6px 0;
    transition: top 0.2s ease;
  }
  .skip-link:focus { top: 0; }
</style>
 </head>
 <body data-logged-in="<%= user ? '1' : '0' %>">
  <a href="#main-content" class="skip-link">Lewati ke konten utama</a>
  <%- include('../partials/navbar', { user, activeNav }) %>
